# GitHub Pages Jekyll configuration
# This allows .md files to be served as static assets
include:
  - "**/*.md"
  - "**/*.json"
  - "blog/**"

# Optional: don't transform files
keep_files:
  - blog
  - blog/*.md
